/**
 * Reserved for Tenancy's HTTP interface (E05-T24..T25), which itself
 * depends on platform HTTP composition seams that don't exist yet (ADR-0012's
 * REST API is a later epic). No route shape is defined here — inventing one
 * now would pre-empt a decision that belongs to E05-T24.
 */
export {};
