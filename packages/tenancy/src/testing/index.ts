/**
 * Reserved for adopter-facing fakes/fixtures (E05-T28), following
 * `@corestack/platform/testing`'s existing subpath convention. The
 * `./testing` export condition is declared in package.json now — not added
 * later — so `@corestack/tenancy/testing` is a stable import path from the
 * module's first release, closing the readiness-gate friction log's step-1
 * finding that module conventions were being decided ad hoc, after the
 * fact, rather than upfront.
 */
export {};
