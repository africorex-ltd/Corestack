{
  "name": "@corestack/kernel",
  "version": "0.2.0",
  "description": "CoreStack shared building blocks: Result, error taxonomy, clock and id ports.",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@corestack/tsconfig": "workspace:*",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
