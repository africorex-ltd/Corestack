# @corestack/cli — placeholder

> **Status: 📋 Planned** — Epic [E15](../../docs/engineering/05-interface.md), Milestone M4.
> Purpose README only; code arrives when the epic starts.

The `corestack` operator CLI — convenience over module APIs, never the only
way: `init` (scaffold composition), `migrate` (per-module migration engine
front-end), `doctor` (config/connectivity/drift diagnostics with fix hints),
`generate` (OpenAPI + typed client), `dev seed`. Non-interactive by default,
`--json` output, secrets never echoed.

Design: [Architecture §29](../../docs/architecture/ARCHITECTURE.md)
