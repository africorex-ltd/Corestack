# apps/reference-nextjs — placeholder

> **Status: 📋 Planned** — Epic [E17](../../docs/engineering/06-apps-docs.md), Milestone M4.
> Purpose README only; the app arrives when the epic starts.

The reference application: a deployed, public Next.js app composing every
CoreStack module — living documentation, the E2E and load-test substrate, and
the home of the "projects" sample feature that teaches the adopter seam
(permissions registered, events audited, entitlements enforced). Never
published to npm; always green on `main`.
